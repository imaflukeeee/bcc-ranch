ConfigAnimals = {
    animalSetup = {
        cows = {
            model = "a_c_cow",
            animalHealth = 100,
            roamingRadius = 100
        },

        pigs = {
            model = "a_c_pig_01",
            animalHealth = 100,
            roamingRadius = 100
        },

        sheeps = {
            model = "a_c_sheep_01",
            animalHealth = 100,
            roamingRadius = 100
        },

        goats = {
            model = "a_c_goat_01",
            animalHealth = 100,
            roamingRadius = 100
        },

        chickens = {
            model = "a_c_chicken_01",
            animalHealth = 100,
            roamingRadius = 100
        }
    }
}